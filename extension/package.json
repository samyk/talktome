{
  "name": "talktome-extension",
  "version": "0.0.8",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:firefox": "tsc --noEmit && BROWSER=firefox vite build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "@crxjs/vite-plugin": "^2.0.0-beta.28"
  }
}
